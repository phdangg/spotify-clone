"use client";

import { Song } from "@/types";

interface LikedContent {
    songs: Song[];
}

const LikedContent: React.FC<LikedContent> = ({
    songs
}) => {
    return (
        <div>

        </div>
    );
}
export default LikedContent;